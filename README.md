
  # Tela de carregamento animada

  This is a code bundle for Tela de carregamento animada. The original project is available at https://www.figma.com/design/e7sdn0dT4VQtZHQQYVb2WO/Tela-de-carregamento-animada.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  